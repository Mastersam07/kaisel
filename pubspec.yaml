name: gate_workspace
publish_to: none

environment:
  sdk: ^3.8.0

# Dart pub workspace. All members share a single resolution and lockfile.
workspace:
  - packages/gate_core
  - packages/gate
  - packages/gate/example
  - packages/gate_devtools
  - packages/gate_lint
